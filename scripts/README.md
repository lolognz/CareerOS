# Scripts

This directory is reserved for automation.

Future scripts may validate the knowledge base, check relations, generate outputs, export documents, or run AI-assisted workflows.

Scripts should treat `knowledge/` as the source of truth and write generated artifacts to `generated/`.
